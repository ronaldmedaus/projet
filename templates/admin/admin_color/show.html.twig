{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
    <h1>Color</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin/admin_color_index') }}">back to list</a>

    <a href="{{ path('admin/admin_color_edit', {'id': color.id}) }}">edit</a>

    {{ include('admin/admin_color/_delete_form.html.twig') }}
{% endblock %}
