{% extends 'admin/base.html.twig' %}

{% block title %}{{ condition.nom }}{% endblock %}
{% block body %}
	<div class="container py-5">
		<div class="row justify-content-center">
			<div class="col-sm-12 col-md-6">
				<div class="card">
					<div class="card-header ">
						<h5>Conditions générales</h5>
					</div>
					<div class="card-body">
						<a href="{{ path('admin_condition_edit',{'nom':'Mentions Légales'}) }}" class="btn btn-info w-100 mt-2">Mentions légales</a>
						<a href="{{ path('admin_condition_edit',{'nom':'Conditions générales de ventes'}) }}" class="btn btn-info w-100 mt-2">Conditions générales de ventes</a>
						<a href="{{ path('admin_condition_edit',{'nom':'Conditions générales d\'utilisations'}) }}" class="btn btn-info w-100 mt-2">Conditions générales d'utilisations</a>
						<a href="{{ path('admin_condition_edit',{'nom':'Retour & Remboursement'}) }}" class="btn btn-info w-100 mt-2">Retour et remboursement</a>
						<a href="{{ path('admin_condition_edit',{'nom':'Protections des données'}) }}" class="btn btn-info w-100 mt-2">Protection des donées</a>
					</div>
				</div>
			</div> 
		</div>
	</div>

{% endblock %}
