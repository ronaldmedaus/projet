{% extends 'admin/base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
<div class="container py-5">

    <div class="row">
    
        <div class="col-12 ">
            <a class="btn btn-dark" href="{{ path('admin_footer') }}">Revenir à la liste CGU</a>
        </div>

        <div class="col-sm-12 mt-3">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">Ajouter les Mentions</h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary mt-3" type="submit">Enregistrer</button>
                    {{ form_end(form) }}
                </div>

                {#<div class="card-body">
                <p class="mb-1">{{ form.imagePath }}</p>
                <p class="mb-1">{{ form.title }}</p>
                <p class="mb-1">{{ form.content }}</p>
                <!--<p class="mb-1">{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</p>-->
                </div>#}
            </div>
        </div>

    </div>
  </div>
 
{% endblock %}