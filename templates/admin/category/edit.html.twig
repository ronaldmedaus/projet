{% extends 'admin/base.html.twig' %}

{% block title %}Edit Category
{% endblock %}

{% block body %}
	<div class="container py-5">
		<div class="row">
			<div class="col-12">
				<a class="btn btn-dark" href="{{ path('admin/category_index') }}"><span class="btn-label"><i class="fa fa-chevron-left"></i></span>Revenir à la liste</a>
			</div>

			<div class="col-sm-12 mt-3">
				<div class="card">
					<div class="card-header">
						<h5 class="card-title"><span class="btn-label"><i class="fa-solid fa-gears"></i></span>Gérer la catégorie</h5>
					</div>
					<div class="card-body">
						{{ include('admin/category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
						{{ include('admin/category/_delete_form.html.twig') }}

					</div>
				</div>
			</div>

		</div>
	</div>


{% endblock %}
